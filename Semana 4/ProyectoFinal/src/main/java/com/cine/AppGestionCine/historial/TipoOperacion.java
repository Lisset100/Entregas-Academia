package com.cine.AppGestionCine.historial;

/**
 * Tipos de operaciones que se registran en el historial
 * Comentario de repaso: Un enum (enumeración) es un tipo especial de Java que permite definir un conjunto limitado y fijo de constantes.
 */
public enum TipoOperacion {
    FUNCION_CREADA,
    FUNCION_CANCELADA,
    ASIENTO_RESERVADO,
    ASIENTO_CANCELADO,
    ASIENTOS_GENERADOS
}
